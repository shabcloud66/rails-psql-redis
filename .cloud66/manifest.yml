staging: # 1. Environment type
  rails: # 2. Application type
    servers:
      server: # 3. Server type (optional)
        vendor: digitalocean
        size: 64
        region: 4
        unique_name: web
  redis:
    servers:
      server:
        vendor: digitalocean
        size: 62
        region: 4
        unique_name: redis
  postgresql:
    servers:
      server:
        vendor: digitalocean
        size: 65
        region: 4
        unique_name: db
  memcached:
    shared_group: web 
    configuration:
      memory: 1024
