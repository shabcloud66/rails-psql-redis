development:
  rails:
    servers:
    - server:
        unique_name: frontend
        type: BYOS
        address: 123.123.123.123
        username: ubuntu
        ssh_key_name: tja
  postgresql:
    servers:
    - server: external
  redis:
    servers:
    - server: external    
  mongodb:
    servers:
    - server: external 
  mysql:
    servers:
    - server: external    
